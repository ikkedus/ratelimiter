 name: addNewTrigger.concurrency
 description: Limits the concurrent executions for ratelimiter.createNewTrigger.
 enabled: true
 resource_ref: ratelimiter.createNewTrigger
 policy_type: action.concurrency
 parameters:
  action: delay
  threshold: 1